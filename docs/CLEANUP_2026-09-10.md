# Repository cleanup — 10 September 2026

Removed 30 tracked, locally unchanged files (5,245,551 bytes) after the supervisor review:

- Superseded top-level methodology, architecture, pitch, benchmark and dissertation documents/PDFs containing inconsistent historical claims. Current methodology and the supervisor review retain the relevant technical account.
- Three PDF generators that embedded unsupported numerical tables.
- The estimated three-class ablation note.
- Two notebooks: the older duplicated audio pipeline and an unrelated external OBD-II/EngineFaultDB launcher.
- The extracted plain-language brief and its outdated 12-class PDF.
- Two byte-identical copies of retained confusion/error figures.
- The display-smoothing script and its six redundant figure exports. Empirical ROC figures and score caches remain.

The complete path list, pre-deletion SHA-256 hashes and Git commit for recovery are in `outputs/supervisor_review/cleanup_manifest.json`. All deleted files existed unchanged in that commit. They can be restored selectively from Git; no archive copy was added to the working tree.

Original audio, frozen partitions, feature caches, experiment results, checkpoints, empirical plots, shared source, tests, project instructions and the supervisor audit remain. Historical experiment scripts are retained because their source is part of the evidence trail. Their presence is not an endorsement of their scientific claims.

The README now identifies the current three-class workflow. The supervisor review and its audit JSON are historical snapshots: references to deleted files identify the pre-cleanup versions, recoverable using the manifest. This cleanup does not repair all methodological issues or constitute a new experiment.
